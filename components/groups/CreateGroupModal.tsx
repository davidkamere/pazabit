import { useState } from "react";
import { Icon } from "@/components/ui/Icon";
export function CreateGroupModal({ onClose, onCreate }: { onClose: () => void; onCreate: (tag: string) => void }) { const [tag, setTag] = useState(""); const normalized = tag ? `#${tag.replace(/^#/, "").replace(/\s/g, "")}` : ""; return <div className="fixed inset-0 z-30 grid place-items-center bg-black/60 p-4 backdrop-blur-sm"><div className="w-full max-w-md rounded-2xl border border-[#3b4140] bg-[#171a1a] p-6 shadow-2xl"><div className="flex justify-between"><div><p className="text-xs font-bold uppercase tracking-[.14em] text-lime">Private by default</p><h3 className="mt-2 text-xl font-bold tracking-[-.04em]">Create action group</h3></div><button onClick={onClose} className="text-muted hover:text-white"><Icon name="close" className="size-5"/></button></div><p className="mt-3 text-sm leading-6 text-muted">Groups are encrypted and require a password to join.</p><label className="mt-5 block text-xs font-bold uppercase tracking-wide text-muted">Group hashtag<input value={tag} onChange={event => setTag(event.target.value)} placeholder="#Evacuation" className="mt-2 w-full rounded-xl border border-line bg-[#1e2222] px-3 py-3 text-sm font-semibold text-white outline-none placeholder:text-[#626967] focus:border-lime"/></label><label className="mt-4 block text-xs font-bold uppercase tracking-wide text-muted">Group password<input type="password" placeholder="Set a password" className="mt-2 w-full rounded-xl border border-line bg-[#1e2222] px-3 py-3 text-sm text-white outline-none placeholder:text-[#626967] focus:border-lime"/></label><button onClick={() => normalized && onCreate(normalized)} disabled={!normalized} className="mt-6 flex w-full items-center justify-center gap-2 rounded-xl bg-lime px-4 py-3 text-sm font-bold text-ink disabled:opacity-40"><Icon name="plus" className="size-4"/> Create private group</button></div></div>; }
